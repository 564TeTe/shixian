# 建库、清理与教学初始化

更新：2026-09-08；本机 MySQL 8.0.41，数据库 t132。

## 当前状态

演示数据已完整备份后清理。原 16 张表保留结构，新增 9 张教学及导入表，共 25 张。已生成 1 个管理员、79 个临时教师、14 间实验室、93 门课程、255 个任务、2,208 条排课，实验项目 0 条。课表暂存 255 行为 PROMOTED，原始值和差异保留。

2025-2026 两学期归档；当前 2026-2027 第一学期开放。当前任务可维护项目，历史项目只读，可复制到同课程当前任务。手动新建任务无虚构排课，实际排课学时为 0。

## 开始使用

1. 运行项目根目录 `start-teaching.ps1`，访问 http://localhost:8081 。
2. 管理员账号 admin，密码见本机 `database/generated/teaching-accounts.local.json`。
3. 在教师与实验室页面核实临时工号、负责人和设备数。
4. 在“课程与课表”创建当前学期任务，或通过导入中心上传当前课表。
5. 为任务录入真实实验项目；在统计报表按学年／学期查询、导出。

教师只能访问参与的任务及项目，不能管理账号、上传整体课表或使用管理员自然语言数据库查询。

## 新机器初始化

安装 MySQL 8、Java、Maven、Node.js、Python。MySQL 客户端内：

```sql
CREATE DATABASE t132 CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE t132;
SOURCE D:/STUDY/iahfbiwabidubIO/T132.sql;
SOURCE D:/STUDY/iahfbiwabidubIO/database/003_teaching_seed.sql;
```

路径换成自己的仓库位置；命令行客户端从仓库根目录启动时也可使用相对路径。Workbench 请先选中目标库，再依次打开并执行这两个 SQL 文件的全部内容。

T132.sql 为纯结构，003_teaching_seed.sql 包含老师课表对应的完整教学数据，无需再找原始 Excel。导入后为 93 门课程、255 个任务、2,208 条排课、79 个教师、14 间课表实验室、1 个批次和 255 条原始导入行，正式实验项目为 0。其他本机后来新增、未关联课表的实验室不纳入这个共享课表快照。

新环境网页管理员账号 `admin`，初始密码 `Teaching2026!`，登录后修改。教师密码使用新随机摘要，管理员重置后才分发。共享 SQL 不包含本机密码摘要、令牌、电话、头像、数据库账号权限或 AI 凭据。本机以前的随机密码不受此脚本影响，因为这里不在本机业务库重新导入。

数据导入使用事务与异常回滚处理：任一 INSERT 出错则整体回滚；非空目标库会明确拒绝，不更新或删除既有数据。需要 MySQL 8，执行者需具有目标库建表、建存储过程和插入权限；临时安装过程在成功后自动删除。失败后如安装过程尚在，可先检查错误并执行 `DROP PROCEDURE IF EXISTS install_teaching_seed_20260908;` 再重试，不要清理已有业务数据。

已有旧库升级使用 001、002 增量脚本，不重复导入纯结构或共享数据。完成新环境导入后，配置自己机器的后端 MySQL 连接，前端执行 npm ci，根目录执行 start-teaching.ps1 -Build 即可启动。

### 可选：从另一份 Excel 重新初始化

将 MySQL bin 加入 PATH，在进程环境中配置 MYSQL_PWD，然后：

```powershell
python -m pip install -r tools/database/bootstrap-requirements.txt
python tools/database/bootstrap_teaching.py --workbook '<真实课表.xlsx路径>' --reset-legacy-data
```

这个命令会清理已有业务数据，仅用于明确重新初始化时。解析、完整备份成功后才开始事务清理，失败回滚；连接参数支持 --host、--port、--user、--database。日常使用网页导入，不重跑清理。

## 数据口径

- TMP 是临时账号，不是学校工号；79 个不同姓名仍需核实人员身份及同名情况。
- 五组身份疑点源行 43/44、45/46、73/74、81/82、221/222 分别保留，不擅自合并。
- 11 个任务计划与排课学时不一致；报表按排课学时计，人时为每条排课学时乘任务选课人数，避免多教师／多项目重复累计。
- 202 行教学班人数与选课人数不同、9 行专业缺失，原值保留。选做项目实际参与人数不能由课程人数推定。
- 项目模板“示例”不作为正式教学内容；学科代码0809保留前导零。
- 相同文件不会重复生成；异文件疑似相同任务需逐行核对，确认独立后生成，不覆盖旧任务。

## 本地备份与验证

清理前备份：`database/generated/before-demo-cleanup-20260908-142716-6b3f44.sql`；原仓库 SQL：`database/generated/original-repository-T132.sql`。备份、真实课表暂存、账号清单和 AI 只读凭据均被 Git 忽略。

当前报告为 `reports/teaching-initialization.json` 和 `reports/teaching-api-smoke.json`。此前 database-application、mysql-verification 等是第一阶段历史报告，其中“仅暂存／尚无页面”不代表当前状态。

测试包括权限隔离、历史保护、复制、真实课表、导入幂等与统计。启用 MySQL 回滚测试设置 TEACHING_DB_TEST=1 及 TEACHING_TEST_DB_URL/USER/PASSWORD，执行 mvn test；HTTP 脚本 finally 只清理自身测试记录。

共享数据验收：`python tools/database/verify_teaching_seed.py`（需要 MYSQL_PWD、MySQL 客户端、Java、已构建后端与 bootstrap-requirements.txt 的依赖）。它在独立临时数据库导入发布文件，检查数量、JSON、人时、重复导入拒绝、故障回滚，再启动临时后端验证新管理员及教师登录；结束后清除自身临时服务和数据库。报告为 `reports/teaching-seed-verification.json`。

维护者需要重新生成共享数据时，可运行 `tools/database/export_teaching_seed.py`。它只读源库、使用字段白名单、新建密码摘要，不导出本机账号凭据。生成后必须重新验收并审阅新增教学数据再提交。
